[package]
name = "pliers-dict"
description = "词库导入工具：拼音词表 + 词频表 → SQLite（带权重）"
version.workspace = true
edition.workspace = true

[dependencies]
turso = { version = "0.7.2", default-features = false }
pollster = "1.0.1"
pliers-engine.workspace = true
