[package]
name = "ime-engine"
description = "输入法引擎：按键 → 决定（纯逻辑，不碰 Wayland）"
version.workspace = true
edition.workspace = true

[dependencies]
pollster = "1.0.1"
serde = { version = "1.0.229", features = ["derive"] }
toml = "1.1.6"
turso = { version = "0.7.2", default-features = false }
